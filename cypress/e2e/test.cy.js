describe('Swag Labs', () => {

    it('1. Login page loads properly and all elements are displayed properly.', () => {

    })

    it('2. Verify if a user will be able to login with a valid username and valid password.', () => {

    })

    it('3. Verify if a user cannot login with an invalid username and an invalid password.', () => {

    })

    it('4. Proper error message is displayed when user tries to login with wrong credentials.', () => {

    })

    it('5. User is able to login successfuly.', () => {

    })

    it('6. User is able to add items to the cart.', () => {

    })

    it('7. User is able to check if items are added to the cart.', () => {

    })

    it('8. User is able to remove item from the cart.', () => {

    })

    it('9. User is able to continue the Checkout.', () => {

    })


})